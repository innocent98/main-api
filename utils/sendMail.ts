// const { forgotPasswordEmail } = require("../configs/forgot_password");

// const sendConfirmationEmails = async (mail) => {
//   // console.log('order', order)
//   try {
//     forgotPasswordEmail((email = mail.senderEmail), (code = mail.senderName));
//     console.log("Email sent");
//   } catch (emailError) {
//     console.error("Error sending confirmation emails:", emailError);
//     // Optionally handle email sending errors
//   }
// };

// module.exports = { sendConfirmationEmails };
